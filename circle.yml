machine:
  services:
    - docker

dependencies:
  # pre:
  #   - sudo /opt/google-cloud-sdk/bin/gcloud --quiet components update --version 120.0.0
  #   - sudo /opt/google-cloud-sdk/bin/gcloud --quiet components update --version 120.0.0 kubectl
  #   - echo $GCLOUD_SERVICE_KEY | base64 --decode -i > ${HOME}/gcloud-service-key.json
  #   - sudo /opt/google-cloud-sdk/bin/gcloud auth activate-service-account --key-file ${HOME}/gcloud-service-key.json
  #   - sudo /opt/google-cloud-sdk/bin/gcloud config set project $GCLOUD_PROJECT
  #   - sudo /opt/google-cloud-sdk/bin/gcloud --quiet config set container/cluster $CLUSTER_NAME
  #   - sudo /opt/google-cloud-sdk/bin/gcloud config set compute/zone ${CLOUDSDK_COMPUTE_ZONE}
  #   - sudo /opt/google-cloud-sdk/bin/gcloud --quiet container clusters get-credentials $CLUSTER_NAME

  override:
    - docker build -t gcr.io/valued-bastion-137423/vocab-trainer-frontend:$CIRCLE_SHA1 .

test:
  override:
    - echo 'see build'

# deployment:
#   gcloud:
#     branch: master
#     commands:
#       - sudo /opt/google-cloud-sdk/bin/gcloud docker push gcr.io/valued-bastion-137423/slack-bot:$CIRCLE_SHA1
#       - sudo /opt/google-cloud-sdk/bin/gcloud components install kubectl
#       - sudo /opt/google-cloud-sdk/bin/kubectl apply -f kubernetes.yaml
#       - sudo /opt/google-cloud-sdk/bin/kubectl patch deployment slack-bot -p '{"spec":{"template":{"spec":{"containers":[{"name":"slack-bot","image":"gcr.io/valued-bastion-137423/slack-bot:'"$CIRCLE_SHA1"'"}]}}}}'